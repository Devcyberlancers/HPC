"# HPC" 
# Root stress and disease detection files integrated with the Interanl api for Testing 
Run pip install -r requirements.txt

# If Encountered with the error while installing the required packages do create the * Virtual Environment and try to install inside the virtual environment *.
**To Install the virtual environment (Optional)
python3 -m venv env

# Activate the virtual environment before do anything
source env/bin/activate

